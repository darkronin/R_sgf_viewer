 - define variable sgf_file
sgf_file = 'C:/temp/somefile.sgf'

 - initialize the parsing "engine" with the following 2 lines verbatim
d2 <- readLines(sgf_file)  # 'd2.sgf'
cd2 <- grep(pattern = '^;[WB]', x = d2, value=TRUE)

- use the following functions: newplot() to initialize the graph, placehandi() to initialize the handicap stones,
playnext() to play one move at a time
newplot()
placehandi()
playnext()
